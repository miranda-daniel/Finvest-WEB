query GetPortfolioDetail($id: Int!) {
  portfolioDetail(id: $id) {
    id
    name
    description
    realizedPnl
    holdings {
      id
      quantity
      avgCost
      realizedPnl
      instrument {
        symbol
        name
        instrumentClass
      }
    }
  }
}
